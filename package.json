{
  "name": "container-homes",
  "version": "0.2.1",
  "private": true,
  "description": "Set a home URL per Firefox container, then jump to it from the toolbar button or Alt+Shift+Home.",
  "license": "MIT",
  "scripts": {
    "lint": "web-ext lint",
    "start": "web-ext run",
    "build": "web-ext build",
    "sign": "web-ext sign --channel unlisted"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
