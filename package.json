{
  "name": "container-homes",
  "version": "0.1.0",
  "private": true,
  "description": "Open a configured home URL for the current Firefox container.",
  "scripts": {
    "lint": "web-ext lint",
    "start": "web-ext run",
    "build": "web-ext build",
    "sign": "web-ext sign --channel unlisted"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
